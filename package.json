{
  "name": "-latest",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && cp robots.txt dist/robots.txt && cp sitemap.xml dist/sitemap.xml && firebase deploy --only hosting"
  },
  "devDependencies": {
    "vite": "^6.0.1"
  },
  "dependencies": {
    "firebase": "^11.3.0"
  }
}
